# FastMCP and core dependencies
fastmcp>=2.10.5
fastapi>=0.115.14
uvicorn[standard]>=0.30.0
starlette>=0.37.0
pydantic>=2.0.0

# HTTP and web scraping
httpx
aiohttp
beautifulsoup4

# Database (MySQL için - CUSTOM DATABASE ONLY)
mysql-connector-python>=8.0.33

# Note: Removed heavyweight packages for Vercel deployment:
# - playwright (170MB+ with browser binaries)
# - markitdown[pdf] (large dependency chain)
# - pypdf (not needed for custom DB)
# These are only needed for public API scraping, not for custom database access

# FastMCP and core dependencies
fastmcp>=2.10.5
fastapi>=0.115.14
uvicorn[standard]>=0.30.0
starlette>=0.37.0
pydantic>=2.0.0

# HTTP and web scraping (lightweight)
httpx
aiohttp
beautifulsoup4

# Database (MySQL için - CUSTOM DATABASE ONLY)
mysql-connector-python>=8.0.33

# Note: playwright removed (170MB+) - using HTTP-only scraping
# markitdown[pdf] and pypdf removed - not needed for API access
